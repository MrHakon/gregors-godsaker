@* Se de rare greiene. p-1? bg-info? Blazor kommer med Bootstrap innebygget. 
Men man kan også lage en CSS-fil som er scopet til denne komponenten, ved opprette filen i samme mappe og gi den eksakt samme navn med .css-ending: ButtonComponent.razor.css, 
eller valgfritt navn og drag-and-drop den oppå ButtonComponent.Razor. Noen ganger virker det også. *@

<div class="p-1">
    <button @onclick="Clicked" class="btn btn-primary">Selvbevisst knapp</button>
    @if (!string.IsNullOrWhiteSpace(AVariableThatIsAvailableInTheWholeFile))
    {
        <p class="mt-2 bg-info p-2 rounded-3">@AVariableThatIsAvailableInTheWholeFile</p>
    }
</div>

@* For små komponenter kan man skrive koden i samme fil, ved å legge på en @code {} *@

@code {

    // Dette er en variabel som hvilken som helst funksjon i denne filen har tilgang på.
    // Kun håp og drømmer håndhever at denne ikke er tuklet med, men i en så liten komponent, er risikoen ganske liten
    private string AVariableThatIsAvailableInTheWholeFile { get; set; } = string.Empty;

    // Se her! God gammel C#, i stedet for JavaScript
    private void Clicked()
    {
        AVariableThatIsAvailableInTheWholeFile = string.IsNullOrWhiteSpace(AVariableThatIsAvailableInTheWholeFile)
            ? "Jeg er en knapp, og skjønner ikke hva jeg gjør som selvstendig komponent, men det kan være fint å kikke på koden min for å se hvordan den ofte er strukturert i Blazor"
            : string.Empty;
    }

}